
float depth_f = sqrt(eyePos.x * eyePos.x + eyePos.y * eyePos.y + eyePos.z * eyePos.z);
const float max_depth = 196.0f;
depth_f = clamp(depth_f / max_depth, 0.0f, 1.0f);
gl_FragData[0] = vec4(1 - depth_f, 1 - depth_f, 1 - depth_f, 1.0f);
gl_FragData[1] = vec4(1 - depth_f, 1 - depth_f, 1 - depth_f, 1.0f);
